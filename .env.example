# --- Obrigatórios ---
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# --- IA (pelo menos um) ---
GEMINI_API_KEY=
GROQ_API_KEY=

# --- Free API pool (fallback para IA) ---
# Opção 1: entries individuais (cada key com modelo próprio)
FREE_API_ENTRIES=
# Opção 2: formato simples (key única, todas com mesmo modelo)
FREE_API_KEY=
FREE_API_BASE_URL=https://aiapiv2.pekpik.com/v1
FREE_API_MODEL=smart-chat

# --- Key Watcher (notificação pessoal) ---
KEYWATCH_CHAT_ID=
# GH_TOKEN=  # PAT com escopo repo (para auto-update FREE_API_ENTRIES)

# --- Email (opcional) ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=
EMAIL_TO=

# --- Google Calendar (opcional) ---
GOOGLE_CALENDAR_CREDS=
GOOGLE_CALENDAR_ID=primary

# --- Filtros (opcional) ---
FILTER_STATES=
MIN_SALARY=
DIGEST_HOUR=20
DIGEST_SOURCES=
SCHEDULE_HOURS=8,15,22
SCHEDULER_TIMEZONE=America/Sao_Paulo
